const menu=document.querySelector('.menu'),nav=document.querySelector('header nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>nav?.classList.remove('open')));const track=(name,params={})=>{window.gtag?.('event',name,params);window.dataLayer?.push({event:name,...params})};track('view_content',{content_name:'sites_landing'});document.querySelectorAll('[data-cta]').forEach(a=>a.addEventListener('click',()=>track('cta_click',{cta:a.dataset.cta})));document.querySelectorAll('[data-contact]').forEach(a=>a.addEventListener('click',()=>track('contact_click',{method:a.dataset.contact})));const form=document.querySelector('#quote-form'),note=document.querySelector('.form-note');form?.addEventListener('focusin',()=>track('form_start',{form_name:'quote'}),{once:true});form?.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return}const d=new FormData(form),body=[`Nom : ${d.get('name')}`,`Email : ${d.get('email')}`,`Téléphone / WhatsApp : ${d.get('phone')||'—'}`,`Activité : ${d.get('business')||'—'}`,`Type de projet : ${d.get('type')}`,'','Message :',d.get('message')].join('\n');track('generate_lead',{form_name:'quote'});note.textContent='Votre client e-mail va s’ouvrir avec votre demande préremplie.';location.href=`mailto:hello@jackbrayan.com?subject=${encodeURIComponent('[Demande de devis] '+d.get('type'))}&body=${encodeURIComponent(body)}`});document.querySelector('#year').textContent=new Date().getFullYear();
